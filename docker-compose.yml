services:
  # =====================================================
  # Redis Database Server Service
  # =====================================================
  redis-server:
    image: docker.io/library/redis:8-alpine
    container_name: ${REDIS_CONTAINER_NAME}
    environment:
      REDIS_ADMIN_NAME: ${REDIS_ADMIN_NAME}
      REDIS_ADMIN_PASSWORD: ${REDIS_ADMIN_PASSWORD}
      REDIS_USER_NAME: ${REDIS_USER_NAME}
      REDIS_USER_PASSWORD: ${REDIS_USER_PASSWORD}
    ports:
      - "${REDIS_EXTERNAL_PORT}:${REDIS_INTERNAL_PORT}"
    volumes:
      - ./app/shared/redis/initialization/01-create-users.acl:/usr/local/etc/redis/01-create-users.acl
    command: redis-server --aclfile /usr/local/etc/redis/01-create-users.acl
    healthcheck:
      test: ["CMD", "redis-cli", "--user", "${REDIS_ADMIN_NAME}", "--pass", "${REDIS_ADMIN_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # =====================================================
  # PostgreSQL Database Server Service
  # =====================================================
  postgres-server:
    image: docker.io/library/postgres:18-alpine
    container_name: ${POSTGRES_CONTAINER_NAME}
    environment:
      POSTGRES_USER: ${POSTGRES_SUPERUSER_NAME}
      POSTGRES_PASSWORD: ${POSTGRES_SUPERUSER_PASSWORD}
      POSTGRES_DB: ${POSTGRES_SYSTEM_DB_NAME}
      POSTGRES_USER_NAME: ${POSTGRES_USER_NAME}
      POSTGRES_USER_PASSWORD: ${POSTGRES_USER_PASSWORD}
      POSTGRES_USER_DB_NAME: ${POSTGRES_USER_DB_NAME}
    ports:
      - "${POSTGRES_EXTERNAL_PORT}:${POSTGRES_INTERNAL_PORT}"
    volumes:
      - ./app/shared/postgres/initialization:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5